# PM 審查 2026-09-07

**🚨 展示站自 2026-09-04 起連續 3 天無新產出、上週 4 項建議零進度、且用真實資料證實 judge 飽和與 quality_alert 失效兩項風險確實存在——本週有一項需要立即排查的活生生中斷。**

> 本審查為外部 fractional PM 視角的每週例行檢視。證據來源：repo 文件、GitHub Issues（`mcp__github__list_issues`）、`gh-pages` branch 的 git 內容（`judge-history.json` / `health-history.json` / `archive/` 與其 commit 歷史）。**`https://zheng0258.github.io/local-agent/` 網域本身這次連 WebFetch 與 curl 都被本環境的 egress 政策擋下（CONNECT 403，非 404）**，因此本次分析改直接讀取同一份資料在 `gh-pages` branch 上的原始檔案（`git show origin/gh-pages:judge-history.json` 等），並對兩個純函數（`health.detect_chronic`、`health.detect_judge_saturation`）餵真實歷史資料實際跑一次，取得與正式 `--health` 輸出等價的判定結果——比上週僅讀資料更進一步，直接驗證了系統自己的判斷邏輯。

## 一句話結論

🚨 有阻塞：展示站最後一次成功產出/部署是 **2026-09-04**（`gh-pages` 最新 commit），今天是 09-07，代表連續 **3 個日曆天完全沒有任何新 archive、judge、health 或 deploy 紀錄**——這正是上週報告點名「全天無 Health Record 不會被 chronic 偵測捕捉」的盲區，這次是**活生生地正在發生**而非歷史案例。此外，本週 main branch 零 commit，上次審查的 4 項建議無一被處理。

## 產品現況

- **核心產品**：`daily-brief` 每日 pipeline（README 自述 14 個 idempotent 步驟：5 來源抓取 → dedup → compress → enrich → digest → tldr → judge → report → save → compose_tg → notify → deploy），純本地 LLM 驅動，門面為 `zheng0258.github.io/local-agent`（gh-pages）。
- **門面狀態（本週新訊號）**：`gh-pages` 最新一次 `deploy: daily brief site` commit 是 **2026-09-04**（`b3fa1d3`），其 `archive/` 目錄最新一篇是 `2026-09-04.html`。往前推，倒數第二次 deploy 是 09-02（跳過 09-03？不，09-03 有 archive 檔但 09-03 當天 deploy commit 也存在於歷史中，最新三次為 09-01/09-02/09-04——**09-03 沒有對應的 deploy commit**，即使 `judge-history.json`／`health-history.json` 都有 09-03 的完整記錄）。09-04 之後（09-05、09-06，以及本次審查執行的 09-07）**完全沒有新內容**。
- **上週回顧**：`main` branch 自 08-31 的上一份 PM 審查 commit（`913f2eb`）之後**零程式碼變更**——一整週沒有任何 commit，上週報告的 4 項建議（P0 缺席日偵測、P1 README 漂移、P1 quality_alert 驗證、P2 triage 標籤）全部原地不動。

## 交付與品質

**Judge 分數趨勢**（`judge-history.json`，`gh-pages` 現存 144 筆，2026-04-14 ~ 2026-09-04）：

- 08-25（issue #24 修復：換 `qwen3.6-35b-a3b` + 收緊 rubric）之後，`faithfulness` 子項首次出現有意義的低分：08-31 = 2、**09-03 = 1**（全歷史最低單項分），09-03 當天 `overall` 隨之降到 **3.3**（全歷史最低 overall）。這證明新 rubric 確實有鑑別力，是修復生效的正面訊號。
- **但把真正的 `detect_judge_saturation()` 函數餵入這 144 筆真實資料跑一次，回傳的判定是「仍然飽和」**：近 30 天（08-06~09-04）內有 **29/30 天（97%）** `overall ≥ 4.0`，超過飽和門檻（≥90%）。也就是說，issue #24 的修復改善了單項分數的鑑別力，但三軸平均出來的 `overall` 仍被幾乎恆為 5 分的 `relevance`／`completeness` 拉回天花板附近，系統自己的飽和偵測器（issue #17 交付）目前若跑 `--health` 仍會標註「judge 飽和」。**這代表 issue #24 並未真正解決 issue #17/#24 想解決的問題，只是把病灶從「全部子項都飽和」縮小成「overall 這個複合指標仍飽和」。**
- **`quality_alert` 欄位 144 筆全部為 `false`（0/144，含 09-03 的 faithfulness=1）。** 讀 `agents/daily_brief/steps/judge.py:112-114` 原始碼確認：`quality_alert` 的觸發條件寫死只看 `completeness < 3`，完全不檢查 `faithfulness`／`relevance`／`overall`。09-03 這筆全歷史最差分數的記錄，`completeness` 剛好是 4（正常），所以無論 `faithfulness` 多差都不會觸發警報。**這不是「資料還不夠」（上週的推測），而是設計本身的判準範圍太窄——已用真實資料證實。**

**來源／遞送可靠度**（`health-history.json`，`gh-pages` 現存 144 筆，2026-04-12 ~ 2026-09-03）：

- 成功率：`vault` 100.0%、`security` 99.3%、`hn` 97.1%、`rss` 96.4%（rss 較晚加入，n=110）、`reddit` 95.6%、`telegram` 94.9%、`hatena` 94.2%。
- 用真實 `health.detect_chronic()` 對截至 09-03 的資料實際跑一次：**目前無任何 subject 達到慢性故障門檻**；近 14 天（08-21~09-03）逐日檢視全部 7 個 subject 皆為 `ok`，來源面本身很健康。
- 7 個日期 `results` 完全為空（`06-22`、`07-02`、`07-07`、`07-08`、`07-18`、`08-05`、`08-06`）——與上週報告一致，**上週建議的「缺席日偵測」尚未實作**，這 7 天仍然只能靠人工發現；本週也**新增了一段更嚴重的缺席**：09-05／09-06（及 09-07 進行中）**連 09-03 那種「有記錄但空的」都沒有——連 `_health-history.json` 本身這幾天大概率都沒被寫入**（因為 gh-pages 09-04 之後沒有新 deploy，這幾天若真的跑過，理應在下一次成功 deploy 時把新記錄一併帶上）。

## Roadmap 與 backlog

- **Issues 總覽**：22 筆（20 closed / 2 open），與上週相同，數字沒有變化——因為本週完全沒有開發活動。
- **上週 4 項建議進度**：
  1. health.py 缺席日偵測（P0）——**未做**。
  2. README 判斷模型描述漂移（P1）——**未做**：`README.md`（3 處）與 `README.zh.md`（2 處）仍寫 `google/gemma-4-e4b`，`config/settings.py:32` 實際預設是 `qwen3.6-35b-a3b`（issue #24 已汰換）。
  3. quality_alert 門檻驗證（P1）——**未做**，但本週用真實資料等於間接驗證完成，結論是「門檻確實有問題」（見上）。
  4. issue #2／#3 補 triage 標籤（P2）——**未做**，兩筆仍只有 `enhancement` 單一標籤，已開單 3 個月又 1 週。
- **新發現的一筆記錄治理債**：`docs/adr-0004-chinese-only` 分支（`d4264b5`，2026-08-19 建立）記錄「展示站維持繁中單語，退場英文 TL;DR」的決策，但**從未合併進 `main`**——諷刺的是，這個決策實際對應的程式碼變更（`aadcfc2` 今日重點改繁中，08-06）比 ADR 早了 13 天就已經上線，等於「先做了、後補文件、文件又沒真的落地」，違反 CLAUDE.md 核心原則 1「所有架構決策寫進 CLAUDE.md／必須版本化」——目前這個決策不算真正進了倉庫這個記錄系統。
- **願景 vs 現實落差（本週擴大）**：CONTEXT.md 對 Showcase Site 的定位是「每日更新以證明系統長期自主運作」，這正是本週最直接被打臉的一點——連續 3 天沒有更新，恰恰動搖了「長期自主運作」這個賣點本身。

## 風險

1. **🚨 進行中的門面停更（新，最高優先）**：`gh-pages` 最後一次部署是 09-04，距今（09-07）已 3 個日曆天無任何新產出，且沒有任何機制（Telegram alert、GitHub issue、health record）記錄或通知這件事——這正是 ADR 0001 承認的盲區「pipeline 整條沒收尾就不會留下任何失敗痕跡」，上週是歷史案例分析，這週是即時發生中且尚未被任何人注意到的實例。
2. **quality_alert 判準範圍過窄，已用資料證實**：只認 `completeness < 3`，`faithfulness`／`relevance`／`overall` 再差都不會觸發警報。09-03 全歷史最差分數（faithfulness=1、overall=3.3）就是不會觸發的活生生反例。
3. **Judge 飽和偵測器目前仍會判定「飽和」**：issue #24 的修復改善了子項鑑別力，但複合 `overall` 指標近 30 天 97% ≥4.0，仍超過飽和門檻——用系統自己的偵測邏輯驗證，issue #24 沒有徹底解決 issue #17 想解決的核心問題。
4. **本週零開發活動 + 上週建議零進度**：若下週再次零進度，代表這份 PM 審查機制本身的建議沒有被實際採納的路徑，其存在的價值會被稀釋。
5. **記錄系統名實不符**：ADR 0004 未合併、README 判斷模型描述連續兩週漂移——兩者都是「倉庫即記錄系統」這條核心工程理念目前實際上没做到的地方。
6. **（延續，未變）單點依賴**：pipeline 綁定單一本機 LM Studio 實例與單一 cron 排程機器，無 fallback；本週的 3 天停更雖無法從雲端直接確認根因，但與此風險描述的故障模式（機器離線/未喚醒）高度吻合。
7. **（延續，未變）backlog 治理債**：issue #2、#3 缺 triage 標籤已 3 個多月。

## 優先排序的建議

| 建議 | 預期影響 | 估計成本 | 優先級 |
|---|---|---|---|
| 立即排查 09-05 起連續無產出的根因（本機 LM Studio 是否存活、`lms ps`、cron 是否執行、`/tmp/daily_brief.log`／`/tmp/load_model.log` 有無錯誤）並補跑 | 修復目前正在發生的門面停更，這是唯一「現在就在發生」的問題，直接影響 CONTEXT.md 定位的「長期自主運作」賣點 | 低（人工檢查 + 視情況手動補跑，本次審查在雲端環境無法代為執行） | **P0** |
| health.py 補上「今天完全沒有任何 artifact／deploy」的偵測（上週已提，本週有真實案例佐證其必要性——若當時已有此機制，這次停更會在第一天就被 escalate，而非等到下次 PM 審查才被發現） | 補上 ADR 0001 承認的盲區，讓「整條沒收尾」不再是唯一偵測不到的失敗模式 | 中（純函數 + 單元測試，讀 deploy commit 或 archive 檔存在性即可，不需改動既有 Step） | P0 |
| 擴充 `quality_alert` 判準，將 `faithfulness`／`relevance`／`overall` 一併納入觸發條件，不只看 `completeness` | 讓剛修好、已證實有鑑別力的新 judge rubric 真正產生警報效果，而非「量表動了、開關沒跟著動」 | 低（`judge.py` 改一個條件式 + 補測試案例） | P1 |
| 修正 `README.md`／`README.zh.md` 的 judge 預設模型描述，對齊 `config/settings.py` 的 `qwen3.6-35b-a3b`（連續第二週被點名，成本極低卻拖了兩週） | 消除文件漂移，維護「倉庫即記錄系統」可信度 | 低（改 5 處文字） | P1 |
| 合併或關閉 `docs/adr-0004-chinese-only` 分支（決策已上線變成程式碼，只差記錄真正落地），並為 issue #2／#3 補上 triage 標籤 | 讓文件與現實對齊、backlog 狀態符合專案自訂的 triage 流程 | 低（純合併／標籤決策，不涉及開發） | P2 |

## 本週建議先做

先排查並修復 **09-05 起連續 3 天的門面停更（P0 第一項）**——這是本次審查唯一「現在正在發生、尚未被任何機制注意到」的真實中斷，其餘建議都是可以等這個修好之後再依序處理的品質與治理債。
